[dev]
51.20.42.161 ansible_user=ubuntu

[staging]
staging-server ansible_host=10.0.1.10 ansible_user=ubuntu

[prod]
# Production IP is also sensitive — store it in group_vars/prod/vault.yml
# and reference it as ansible_host="{{ vault_prod_host }}" if needed.
# For now the IP is kept here but SSH credentials are fully vaulted.
app-server ansible_user=ubuntu

[web:children]
dev
staging
prod