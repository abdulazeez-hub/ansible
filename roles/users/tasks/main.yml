---
- name: Create devops user
  user:
    name: devops
    shell: /bin/bash
    create_home: yes
    state: present

- name: Add devops user to sudo group
  user:
    name: devops
    groups: sudo
    append: yes